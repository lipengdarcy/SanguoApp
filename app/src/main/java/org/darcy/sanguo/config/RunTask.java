package org.darcy.sanguo.config;

import org.json.JSONException;

//d
public class RunTask implements Runnable {
    public void run() {
        Message.status status = null;
        if (status != Message.status.a)
            return;
    }
}